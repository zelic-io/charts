apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brotherscanner.fullname" . }}-test-connection"
  labels:
    {{- include "brotherscanner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brotherscanner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
